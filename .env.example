# Copy this into Vercel's Environment Variables (Project Settings -> Environment Variables).
# Point this at your Railway backend's public URL, with /api on the end.
VITE_API_BASE_URL=https://your-railway-service.up.railway.app/api
